{
  "name": "arkham-project",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "yarn workspace @arkham/auth build && yarn workspace microservice-a build && yarn workspace microservice-b build",
    "start:a": "yarn workspace @arkham/auth build && yarn workspace microservice-a start"
  },
  "workspaces": [
    "libs/*",
    "microservice-*"
  ]
}
